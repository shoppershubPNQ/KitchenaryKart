'use client';
import Link from 'next/link';
import {useMemo,useState,useEffect} from 'react';
import {useRouter,useSearchParams} from 'next/navigation';
import {ProductCard} from './ProductCard';
import {CATEGORY_SHORT,catLabel} from '@/lib/categories';
import type {PublicProduct} from '@/lib/products';
const PAGE_SIZE=24;
interface Props{products:PublicProduct[];categoryCounts:Record<string,number>;collectionLabel?:string|null;collectionSlug?:string|null;}
export function ShopView({products,categoryCounts,collectionLabel=null,collectionSlug=null}:Props){
const router=useRouter();const params=useSearchParams();
const[cat,setCat]=useState<string>(params.get('cat')??'');
const[sub,setSub]=useState<string>(params.get('sub')??'');
const[q,setQ]=useState<string>(params.get('q')??'');
const[sort,setSort]=useState<string>('featured');
const[page,setPage]=useState<number>(1);
useEffect(()=>{const sp=new URLSearchParams();if(collectionSlug)sp.set('collection',collectionSlug);if(cat)sp.set('cat',cat);if(sub)sp.set('sub',sub);if(q)sp.set('q',q);const qs=sp.toString();router.replace(qs?`/shop?${qs}`:'/shop',{scroll:false});},[cat,sub,q,router,collectionSlug]);
const filtered=useMemo(()=>{const needle=q.trim().toLowerCase();let list=products.slice();if(cat)list=list.filter((p)=>p.category===cat);if(sub)list=list.filter((p)=>p.subcategory===sub);if(needle){list=list.filter((p)=>p.name.toLowerCase().includes(needle)||p.sku.toLowerCase().includes(needle)||(p.subcategory||'').toLowerCase().includes(needle));}switch(sort){case 'price-asc':list.sort((a,b)=>a.price-b.price);break;case 'price-desc':list.sort((a,b)=>b.price-a.price);break;case 'name':list.sort((a,b)=>a.name.localeCompare(b.name));break;}return list;},[products,cat,sub,q,sort]);
const shown=filtered.slice(0,page*PAGE_SIZE);
const catEntries=Object.entries(categoryCounts).sort((a,b)=>b[1]-a[1]);
const total=products.length;
const crumbCurrent=collectionLabel?`${collectionLabel}${cat?' · '+catLabel(cat):''}${sub?' · '+sub:''}`:cat?`${catLabel(cat)}${sub?' · '+sub:''}`:'Shop';
return(<><nav className="max-w-site mx-auto px-[6mm] md:px-[1.5cm] py-4 text-xs text-muted flex items-center gap-2 flex-wrap"><Link href="/" className="hover:text-brand">Home</Link><span className="opacity-60">/</span><span className="text-ink font-medium">{crumbCurrent}</span></nav><div className="max-w-site mx-auto px-[6mm] md:px-[1.5cm] pb-14 grid gap-8 md:grid-cols-[240px_1fr] grid-cols-1"><aside className="md:sticky md:top-[132px] md:self-start md:max-h-[calc(100vh-140px)] md:overflow-y-auto pr-1"><div><h4 className="text-[11.5px] font-bold tracking-[1.5px] uppercase text-ink mb-3">Category</h4><button type="button" className={`filter-item ${cat===''?'is-active':''}`} onClick={()=>{setCat('');setSub('');setPage(1);}}><span>All categories</span><span className="filter-count">{total}</span></button>{catEntries.map(([c,n])=>(<button key={c} type="button" className={`filter-item ${cat===c?'is-active':''}`} onClick={()=>{setCat(c);setSub('');setPage(1);}}><span>{CATEGORY_SHORT[c]??c}</span><span className="filter-count">{n}</span></button>))}</div></aside><main>{collectionLabel&&(<div className="mb-4 flex items-center gap-2 flex-wrap"><span className="text-xs font-bold tracking-[2px] uppercase text-brand">Showing</span><span className="inline-flex items-center gap-2 bg-brand/10 text-brand px-3 py-1 rounded-full text-sm font-medium">{collectionLabel}<Link href="/shop" className="text-brand hover:text-brand-dark" aria-label="Clear collection filter" title="Show all products">×</Link></span></div>)}<div className="flex items-center justify-between pb-5 border-b border-line mb-6 gap-4 flex-wrap"><input type="search" placeholder="Search…" value={q} onChange={(e)=>{setQ(e.target.value);setPage(1);}} className="px-3.5 py-2 border border-line rounded-md text-sm min-w-[260px] focus:border-brand focus:ring-1 focus:ring-brand outline-none"/><div className="text-sm text-muted">{filtered.length.toLocaleString('en-IN')} products</div><select value={sort} onChange={(e)=>setSort(e.target.value)} className="px-3.5 py-2 border border-line rounded-md text-sm bg-white"><option value="featured">Featured</option><option value="price-asc">Price: low to high</option><option value="price-desc">Price: high to low</option><option value="name">Name A–Z</option></select></div>{shown.length===0?(<div className="py-16 text-center text-muted"><h3 className="text-ink mb-2 font-head font-bold">No products match</h3><p>Try a different category or search term.</p></div>):(<><style jsx>{`.kk-shop-grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:0.75rem;}@media (min-width:768px){.kk-shop-grid{grid-template-columns:repeat(auto-fill,minmax(230px,1fr));gap:1rem;}}`}</style><div className="kk-shop-grid grid">{shown.map((p)=>(<ProductCard key={p.sku} product={p}/>))}</div></>)}{shown.length<filtered.length&&(<div className="text-center mt-9"><button type="button" onClick={()=>setPage((p)=>p+1)} className="px-8 py-3 font-head text-xs font-bold tracking-wider uppercase text-ink bg-white border-2 border-ink rounded-md hover:bg-ink hover:text-white transition">Load more</button></div>)}</main></div></>);}
